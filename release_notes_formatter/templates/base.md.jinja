>Reference > Release Notes > [All revisions] > Revision {{ revision.arm_22_04 }}/{{ revision.amd_22_04 }}

# Revision {{ revision.arm_22_04 }}/{{ revision.amd_22_04 }}

Dear community,

Canonical's newest Charmed {{ display_name }} operator has been published in the [{{ channel }} channel].

Due to the newly added support for `arm64` architecture, the {{ display_name }} charm now releases multiple revisions simultaneously:
* Revision {{ revision.amd_22_04 }} is built for `amd64` on Ubuntu 22.04 LTS
{% if revision.amd_20_04 %}* Revision {{ revision.amd_20_04 }} is built for `amd64` on Ubuntu 20.04 LTS{% endif %}
* Revision {{ revision.arm_22_04 }} is built for `arm64` on Ubuntu 22.04 LTS
{% if revision.arm_20_04 %}* Revision {{ revision.arm_20_04 }} is built for `arm64` on Ubuntu 20.04 LTS {% endif %}

To make sure you deploy for the right architecture, we recommend setting an [architecture constraint](https://juju.is/docs/juju/constraint#heading--arch) for your entire juju model.

Otherwise, it can be done at deploy time with the `--constraints` flag:
```shell
juju deploy {{ app }}{% if substrate == 'k8s' %}-k8s --trust{% endif %} --constraints arch=<arch> 
```
where `<arch>` can be `amd64` or `arm64`.

---

## Highlights 
{{ commits.jira }}
{{ commits.other }}

## Bugfixes

<!--TODO: Fill in manually based on Highlights section.--> 

## Dependencies and automations
{{ commits.bot }}

## Technical details
This section contains some technical details about the charm's contents and dependencies. 

If you are jumping over several stable revisions, check [previous release notes][All revisions] before upgrading.

### Requirements
This revision was tested with:
{% if min_juju %}* Juju `v.{{ min_juju }}` to `v.{{ max_juju }}`.{% endif %}
{% if cloud_version %}* {{ cloud_type }} `v.{{ cloud_version }}`.{% endif %}

See the [system requirements] for more details about Juju versions and other software and hardware prerequisites.

### Packaging
{% block packaging %}
{% endblock %}

### Libraries and interfaces

This charm revision imports the following libraries:
{% block libraries %}
{% endblock %}
See the [`/lib/charms` directory on GitHub] for more details about all supported libraries.

See the [`metadata.yaml` file on GitHub] for a full list of supported interfaces.

<!-- Topics -->
[All revisions]: {{ topic.all_revisions }}
[system requirements]: {{ topic.system_requirements }}

<!-- GitHub -->
[`/lib/charms` directory on GitHub]: https://github.com/canonical/{{ repo_name }}/tree/main/lib/charms
[`metadata.yaml` file on GitHub]: https://github.com/canonical/{{ repo_name }}/blob/main/metadata.yaml

<!-- Charmhub -->
[{{ channel }} channel]: https://charmhub.io/{{ app }}?channel={{ channel }}

<!-- Snap/Rock -->
[`charmed-{{ app }}` packaging]: https://github.com/canonical/charmed-{{ app }}-{{ packaging }}
{% block links %}
{% endblock %}

<!-- Badges -->
[amd64]: https://img.shields.io/badge/amd64-darkgreen
[arm64]: https://img.shields.io/badge/arm64-blue
